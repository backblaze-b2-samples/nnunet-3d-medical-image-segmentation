# Backblaze B2 (required) — S3-compatible credentials.
B2_APPLICATION_KEY_ID=your_key_id
B2_APPLICATION_KEY=your_application_key
B2_BUCKET_NAME=your-bucket-name
# Region determines the S3 endpoint (https://s3.<region>.backblazeb2.com).
# Required — set it to match your bucket's region (no default in source).
B2_REGION=us-west-004

# Optional. Base URL for building public object links (public buckets only).
# The app runs fine without it. Example: https://your-bucket.s3.us-west-004.backblazeb2.com
# B2_PUBLIC_URL_BASE=

# Example of what filled-in values look like (placeholders — replace with yours):
# B2_APPLICATION_KEY_ID=<keyID from the B2 app-keys page>
# B2_APPLICATION_KEY=<applicationKey shown once by B2>
# B2_BUCKET_NAME=my-imaging-cohort
# B2_REGION=us-west-004

# --- nnU-Net segmentation engine ---
# Inference/training device: auto (CUDA -> Apple MPS -> CPU), or force cpu/cuda/mps.
# nnU-Net falls back MPS -> CPU when a 3D op is unsupported; never GPU-required.
# NNUNET_DEVICE=auto
# Prefix under which the trained checkpoint tarball is archived on B2.
# MODEL_PREFIX=checkpoints/

# Frontend → API base URL. Defaults to http://localhost:8000 for local dev;
# set this to the deployed API origin in production (Next.js inlines it at
# build time, so it must be present when `pnpm build` runs).
# NEXT_PUBLIC_API_URL=https://your-api.example.com
